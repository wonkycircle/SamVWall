/// @description timer_subMillis(millis, timer)
/// @param millis
/// @param  timer
function timer_subMillis(argument0, argument1) {

	var timer = argument1;

	// Subtracts the specified amount of milliseconds from the timer.
	timer.currentTime -= argument0;



}

/// @description timer_addMillis(millis, timer)
/// @param millis
/// @param  timer
function timer_addMillis(argument0, argument1) {

	var timer = argument1;

	// Add the specified amount of milliseconds to the timer.
	timer.currentTime += argument0;



}
